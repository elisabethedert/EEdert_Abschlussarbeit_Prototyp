user  nginx;
worker_processes  auto;

error_log  /var/log/nginx/error.log notice;
pid        /var/run/nginx.pid;


events {
    worker_connections  1024;
}


http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    sendfile        on;
    #tcp_nopush     on;

    keepalive_timeout  65;

    #gzip  on;

#    include /etc/nginx/conf.d/*.conf;

  server {
      listen 80;
      server_name *.th-luebeck.de;
      return 301 https://$host$request_uri;
  }

  server {
      listen 443 ssl;
      server_name ei-dmz-vm-srv188.th-luebeck.de;
      ssl_certificate     /certs/ei-dmz-vm-srv188.th-luebeck.de_cert_chained.pem;
      ssl_certificate_key /certs/ei-dmz-vm-srv188.th-luebeck.de_key-no-pw.pem;
      ssl_prefer_server_ciphers on;

      client_max_body_size 16M;

      location / {
          proxy_pass http://ohmrent:80;

          proxy_set_header        Host $host;
          proxy_set_header        X-Real-IP $remote_addr;
          proxy_set_header        X-Forwarded-For $proxy_add_x_forwarded_for;
          proxy_set_header        X-Forwarded-Proto $scheme;
      }

   }

}


